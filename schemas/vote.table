CREATE TABLE vote (
  id VARCHAR(32) PRIMARY KEY,
  congress INT,
  chamber VARCHAR(8),
  session INT,
  rollCall INT,
  billID VARCHAR(16),
  question TEXT,
  description LONGTEXT,
  type VARCHAR(16),
  datetime DATETIME,
  result TEXT
);
