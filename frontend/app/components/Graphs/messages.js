/*
 * Graphs Messages
 *
 * This contains all the text for the Graphs component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Graphs";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Graphs component!"
  }
});
